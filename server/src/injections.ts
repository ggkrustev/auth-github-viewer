import { Container } from 'inversify'

// import DI from './di';
// import { FooService } from './services/foo.service';

const container = new Container()

// container.bind<FooService>(DI.FooService).to(FooService);

export default container

